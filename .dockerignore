# Dependencies (reinstalled via `npm ci` in the Dockerfile)
node_modules
.pnp
.pnp.*

# Next.js build output (regenerated by `npm run build`)
.next
out
build

# Standalone deploy artifact (contains its own .env with secrets)
deploy

# Version control
.git

# Environment files (secrets). Keep only the example.
.env
.env.*
!.env.example

# TypeScript build cache
*.tsbuildinfo
