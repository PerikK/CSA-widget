# Base URL of the upstream Enora API (no trailing slash).
# Local development: http://localhost:8080
UPSTREAM_URL=https://api.enora-oah.eu

# Credentials used server-side to obtain a JWT (POST /api/auth/login).
UPSTREAM_USERNAME=
UPSTREAM_PASSWORD=
